import logging

logging.basicConfig(format=u'[%(levelname)-4s] [%(asctime)s] [LINE:%(lineno)-3s] [%(filename)-15s] %(message)s ',
                    level=logging.INFO,
                    # level=logging.DEBUG,  # Можно заменить на другой уровень логгирования.
                    )
